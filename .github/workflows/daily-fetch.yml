name: Daily Auto Fetch Documents

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

# ↓ この3行を追加します（書き込み権限を明示）
permissions:
  contents: write

jobs:
  fetch-and-commit:
    runs-on: ubuntu-latest
    # （以下省略）